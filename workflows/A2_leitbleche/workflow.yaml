title: "Guide-vane bend -- concentric vanes in a 90deg duct"
description: "CFDPredict guide-vane case with vanes concentric about a common bend centre. Built for optimiser and ROM use (per vane a radial position and an extension, vanes can be switched off continuously). 2D or 3D, with automatic layers for a Reynolds-independent y+. Results from the simpleFoam k-omega-SST run: kinematic pressure loss dP, outlet vorticity and outlet uniformity."
entry_script: py_A2_leitbleche.py
python_exec: ""
input_filename: interface.json
schema_file: interface.json
inject_schema: true
version: v2512
uploads: false

# Soft-lock: warnt (Override-Checkbox noetig), wenn dieser User schon so viele
# laufende Jobs dieses Workflows hat. 0 = aus. Admins ignorieren.
max_concurrent_per_user: 1
