title: "Guide-vane bend -- pathline vanes in a 90deg duct"
description: "CFDPredict guide-vane case with streamline-parallel vanes: the bend walls are parallel-translated, the vanes span the full duct height and form continuous lanes from inlet to outlet. Configure R/H, Reynolds number, number of vanes, trail length and mesh resolution in the browser; single case or vane-count sweep. Results: kinematic pressure loss dP, outlet vorticity and outlet uniformity of the simpleFoam k-omega-SST solution."
entry_script: py_A2_brand_topology.py
python_exec: ""
input_filename: interface.json
schema_file: interface.json
inject_schema: true
version: v2512
uploads: false

# Soft-lock: warnt (Override-Checkbox noetig), wenn dieser User schon so viele
# laufende Jobs dieses Workflows hat. 0 = aus. Admins ignorieren.
max_concurrent_per_user: 1
