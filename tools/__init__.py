"""Headless / non-Streamlit driver for the demo workflows.

Sibling to the workflows/ tree. The Streamlit portal does NOT depend on
this package -- it's for:
  - the Dresden partner package (Bayes-loop driver on their cluster),
  - RunGui-next local-workstation integration,
  - offline smoke tests and CI.
"""
